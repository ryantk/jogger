<%= render 'new', journal: @journal %>

<div class="panel panel-flat">
  <div class="panel-body">
    <p class="center">
      <%= link_to("View all entries in this journal", journal_path(@journal)) %>
    </p>
  </div>
</div>
